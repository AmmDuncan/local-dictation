# Local Dictation Memory

## SHIPPED (2026-06-22) — Draft-preview cue RELEASED as v0.4.4
- Branch ff-merged to **main @ `2fcf8a7`**; **release v0.4.4** (build 55) published (Latest) — dmg + zip + signed Sparkle appcast; auto-update live. Tag → `2fcf8a7`.
- WHAT `2fcf8a7` **feat(overlay)** — the live preview overlay now shows a **"Draft · refining…" eyebrow** above the rolling transcript. The preview is a fast ~900ms partial (Whisper + cleanup only); the final insertion re-transcribes the full audio and runs the correction pipeline (mishearing/command fixes → AI polish → text replacements → smart spacing), so the text changes on release. The eyebrow makes that change read as expected, not a glitch. Listening card height bumped 204→226 for the row.
- NOTE on the preview/final divergence (Ammiel asked): two separate processors by design — preview = speed, final = quality+corrections. Considered applying deterministic corrections to the preview too; chose the lighter "mark as draft" treatment instead.
- DEFERRED follow-ups (Ammiel "not ready" / parked): polish pass could fix article errors ("a"↔"the") + terminal "?" from grammar (both are inherent Whisper limits, not bugs — Whisper keys on grammar not intonation); aligning preview text with deterministic corrections.

## SHIPPED (2026-06-22) — Orphaned-helper reap RELEASED as v0.4.3 (another agent)
- `6339029` **fix(servers)** — reap orphaned whisper/llama helper servers on launch + hard shutdown on quit. Fixes the leak filed this session (helpers orphaned to launchd on quit; ~18 had piled up saturating the GPU → multi-second dictation lag). Adds `HelperReaper` (+2 Core tests → 65 total). Cut by the background task agent spawned earlier. v0.4.4 rebased on top of this.

## SHIPPED (2026-06-22) — UI revamp + built-in-mic preference RELEASED as v0.4.2
- Branch ff-merged to **main @ `2772517`**; **release v0.4.2** (build 52) published (Latest) — dmg + zip + signed Sparkle appcast; auto-update live. Tag → `2772517`.
- WHAT (two commits): **fix(audio)** `2772517` — "System Default" input now **prefers the built-in mic over Bluetooth** (BT mic = HFP/SCO call mode, slow/unreliable SCO negotiation → "System Default" + earbuds captured nothing). Pure policy `AudioInputSelection.choose` (Core, unit-tested, 63 tests); `AudioDevices.resolveInputDeviceID` reads CoreAudio transport + OS default; used by recorder + level meter. An **explicit device pick is still respected**. Also: **level meter now recreates its `AVAudioEngine` per start** — `kAudioOutputUnitProperty_CurrentDevice` only binds on an *uninitialized* HAL unit (the recorder already did this; the meter didn't, which is why explicit-built-in "worked" only because it coincided with the system default during testing).
- WHAT **feat(ui)** `bb69736` — menu-bar popover: bordered buttons → full-width emerald **menu rows** (SF Symbols, hover highlight, ⌘,/⌘Q), stable title + inline colored status, emerald mic. Settings **revamp**: `.tint(Brand.emerald)` app-wide + cohesion sweep (all `.green`/accent → `Brand.emerald`); calmed HealthStrip (neutral card + emerald seal/pills + thin state border); Models capability meta-chips + emerald active/recommended; Audio level-meter hero + "speak to test"; Advanced path overrides behind a disclosure; Learn built-in lists behind disclosures; "Experimental" → caption (shared `ExperimentalLabel`).
- DESIGN: reviewed by the macOS-design-lead persona (easel sign-off, "approved to ship"). simplify pass run (4 agents) — inlined a redundant resolver wrapper, extracted `ExperimentalLabel` + `menuGroup`, dropped needless `AnyShapeStyle`.
- VERIFIED mic-less: **63 Core tests pass**; `swift build` + `build-app.sh` green; device-selection fix mechanism-proven via CoreAudio probes (policy returns built-in on BT default; `CurrentDevice` binds on a fresh engine). **NOT yet live-mic verified** — Ammiel to mic-test in the morning (hold ⌃Space with earbuds as default → should bind built-in, Listening shows, text types).
- RELATED BUG (filed as background task, NOT in this release): whisper-server/llama-server **helpers orphan on app quit** — ~18 stale servers found saturating GPU (caused multi-second lag earlier this session); cleaned manually. Fix: reap on quit + on launch.

## SHIPPED (2026-06-22) — Polish default → Qwen 3.5 4B RELEASED as v0.4.1
- `feat/bigger-polish-models` ff-merged to **main @ `84ded02`**; **release v0.4.1** published (Latest) — dmg + zip + signed Sparkle appcast; auto-update live. Branch deleted.
- WHAT: refreshed the optional LLM-polish catalog to current-gen **Qwen 3.5 4B** (recommended default; bartowski `Qwen_Qwen3.5-4B-Q4_K_M.gguf`), removing the stale Qwen 2.5-3B + Gemma 3. Qwen 3.5 is hybrid-thinking → `TranscriptPolisher.chatRequestBody` now sends `chat_template_kwargs.enable_thinking=false` (its `<think>` blocks would trip `preservesContentWords`); harmless for non-Qwen3.
- VALIDATED via a 6-model ~4B showdown (harness `/tmp/ld-showdown`, REPORT.md): Qwen 3.5 4B is the most faithful polish — removes every filler, drops no words, doesn't over-format — vs Gemma 4 E4B (over-formats `3pm→3 p.m.`, wrong-corrects `clot→Clojure`), Qwen 2.5 3B (under-corrects + dangerously substitutes `clot→clipboard/Colt`), Phi-4-mini (mangles "you know"), Ministral 3 3B (DROPS words: "so I was"→"So was"). 0 `<think>` leaks confirmed on the bundled llama-server. Bonus probe: no ~4B model reliably recovers "Claude" from a mishearing without the vocab → vindicates the formatting-only, guard-blocked polish design (the vocab bias / mishearing map is safer than model knowledge).
- PARKED BUG found while testing: the app does NOT kill its polish `llama-server` child on quit → orphans accumulate (killed 11 during this session). Fix `LlamaServerManager` teardown on quit. Other parked follow-ups: phrase-level command-mode chips, drag-select in review panel, live-re-insertion focus-guard.

## SHIPPED (2026-06-21) — Learn/correction feature (review & teach) RELEASED as v0.4.0
- `feat/learn-corrections` fast-forward-merged to **main @ `ae17f30`**; **release v0.4.0** published (Latest) — DMG + zip + signed Sparkle appcast; installed apps auto-update. Branch deleted post-merge.
- WHAT SHIPPED: on-device review + teach of dictation corrections. **Door #1**: ⌃⌥Z opens a floating review panel for the last dictation. **Door #2**: Settings → Learn tab (recent-dictations queue, taught-rule rows, built-in on/off toggles). Review panel rebuilt to the brand design system (`Brand.swift` extracted from OverlayView): glass card, emerald accent, removable `× revert` change chips (HEARD badge), "Correct … to:" editor with a ✓ Learned confirmation, click/two-tap span selection (drag removed — fought the panel's window-drag), bounded scrollable sentence. Span-selection logic → pure unit-tested `SpanSelection` (Core). Learn tab: bounded scrollable recent list (changes-only); built-ins split into Mishearings vs Command-mode (terminal git) with explainers. Menu-bar last-transcript preview opens History on click.
- REVIEW SHORTCUT: default **⌥Z → ⌃⌥Z** (⌥Z clashed with CleanShot on Ammiel's machine; ⌃Z is terminal SIGTSTP), now **configurable** via a General-tab `KeyboardShortcuts.Recorder`.
- DECISIONS: chips stay **word-level** (`me → main`, truthful to the engine); **live re-insertion experimental/OFF** by default (focus-footgun: could replace the wrong field if focus moved). Both improvable later.
- VERIFIED: **62 Core tests** (added SpanSelection coverage); mic-less pipeline 6/6 on the release build; learn round-trip CONFIRMED live (× revert → suppression persists → next dictation no longer swaps). Off-screen ImageRenderer design-shot hook (`LD_PANEL_SHOT`, env-gated, release-available) added for panel design review. swift build + build-app.sh green.
- FOLLOW-UPS (NOT in v0.4.0): **bigger polish models** (Qwen 2.5 7B 4.7GB sha `65b8fcd9…1423` / 14B 9.0GB sha `e47ad95d…c008`, both bartowski Q4_K_M) for the Models-tab picker → v0.4.1 (in progress); phrase-level chips for command-mode swaps; drag-select; live-re-insertion focus-guard (the parked insertion-target guard).

## SHIPPED (2026-06-21) — Faithful recognition (no history feedback-loop) + no cold-start clip RELEASED as v0.3.3
- `fix/history-feedback-loop` fast-forward-merged to **main @ `82f3116`**; **release v0.3.3** published (Latest) — DMG + zip + signed Sparkle appcast; installed apps auto-update. Bundles BOTH remaining open bugs' fixes. Branch deleted post-merge.
- FIX 1 — **recognition feedback loop removed** (`cdf1493`): the app fed a rolling window of recent transcripts back into whisper's context prompt, so one bad transcript biased the next toward the same error (Ammiel's "Also"→"10"). Recognition now biases ONLY from curated vocabulary + live caret/visible context (sources that can't accumulate pollution). Removed `useHistoryContext` setting + AdvancedTab toggle + the rolling `history` buffer + `RecognitionContext.appendingHistory` (+ its test). Display history (`TranscriptHistoryStore`) untouched — it was only ever display, never fed back. `RecognitionContext.prompt` keeps a `history:` param (defaults `[]`) for completeness; the app passes none.
- FIX 2 — **cold-start LEADING-clip fixed** (`82f3116`): the first words of the first dictation after launch were dropped while the mic/CoreAudio chain warmed up (`engine.start()` returned before the tap streamed audio). `AudioFileRecorder.startRecording` now `await`s the first real mic buffer (`awaitFirstAudio`, `CheckedContinuation`; race-safe — both resumers nil the waiter under `lock` so never a double-resume; bounded `firstAudioTimeoutMillis=1500` so a dead mic can't hang the start). `AppModel.beginHold` shows "Listening" (the cue to speak) only once that returns — so the cue lines up with when the mic is actually live.
- VERIFIED: **51 unit tests** pass; both diffs reviewed (race-safe continuation; zero residual refs to removed history code); build+sign green. **LIVE COLD-MIC test by Ammiel PASSED ×3** — cold relaunch → first dictation "Also the server is fine." landed the first word intact every time. That same run finally LIVE-confirmed v0.3.1 #3 tail-pad + #4 normalize (previously harness-only).
- CLOSES every open item carried from v0.3.0/v0.3.1/v0.3.2. Deferred decoder tuning #5 beam / #6 thresholds / #7 mc stay LOW urgency (campaign saw zero repetition loops) — not scheduled.

## SHIPPED (2026-06-21) — Polish faithfulness (no fabricated coherence) RELEASED as v0.3.2
- `fix/polish-faithfulness` fast-forward-merged to **main @ `c47e57a`**; **release v0.3.2** published. Fixes a bug Ammiel caught: the LLM polish pass (he runs `polishWithAI=1`) rewrote broken/partial speech into grammatical-but-wrong sentences (added "do", dropped "there are", "gonna"→"going to"). Reproduced against his live llama-server with the real polish prompt.
- FIX (Sources/LocalDictationCore/TranscriptPolisher.swift): new **`preservesContentWords`** guardrail — a polish is accepted only when its content words are a SUBSEQUENCE of the original (nothing added/substituted/reordered); only fillers (`droppableFillers`) + stutters droppable; punctuation incl. the new `…` disfluency marker ignored → on reject, keep rules-cleaned text. New **prompt + few-shot**: mark trailed-off/abandoned thoughts with `…`, never complete them, keep "gonna" as-is (Ammiel's ellipsis idea). Removed the superseded length-only `isFaithfulCorrection` (+ its test) — its "may swap misheard words" premise was what let fabrication through. `simplify` ran (4 agents): also documented `droppableFillers` couplings.
- VERIFIED MIC-LESS: **51 unit tests** (the observed fabrications now rejected; filler/stutter/ellipsis accepted) + an integration replay through the live polish model — inputs that fabricated now stay faithful and mark disfluency with `…`. Clean input still polishes, no ellipsis spam. The new prompt did most of the work; the guard is the proven safety net.
- STILL OPEN (NOT in v0.3.2): **cold-start LEADING-clip bug** (first words of the FIRST dictation of the day dropped — AVAudioEngine warmup) — needs a LIVE cold-mic test, so it's the next task (→ v0.3.3). Also #3 tail-pad + #4 normalize (v0.3.1) still want a live-mic confirm. Deferred decoder tuning #5/#6/#7 (campaign saw zero repetition → low urgency).

## SHIPPED (2026-06-21) — Turbo-tuned dictation accuracy RELEASED as v0.3.1
- `feat/turbo-accuracy` fast-forward-merged to **main @ `db9141f`**; **release v0.3.1** published (Latest) — DMG + zip + signed Sparkle appcast; installed apps auto-update. v0.3.1 (build 30) installed + running at /Applications (confirmed live: whisper-server launches with `-vp 200 -vspd 100`). Tuned for large-v3-turbo (no quant).
- KEY REFRAME: the real final-pass decoder is the resident **whisper-server**, NOT the CLI — it was already beam-1/best-of-2, fallback-ON, VAD-ON, language-AUTO. So the gaps were elsewhere. Four fixes: **#1** default language auto→en + one-time migration (`LanguageDefaultMigration`, Core) — turbo mis-detects on 1–3s clips; **#2** loosen Silero VAD `-vp 200 -vspd 100` (shared `WhisperVAD`, server + CLI) — stock 30ms pad / 250ms min-speech dropped short words/edges; **#3** 250ms recorder tail-pad before tap teardown — key-up clipped the last word; **#4** peak-normalize ~-1 dBFS (`AudioNormalizer`, Core) — quiet mic made turbo confabulate. `simplify` clean. **51 unit tests pass.**
- VERIFIED: #1/#2 by real whisper-cli/turbo runs + an overnight mic-less campaign (~627 transcriptions: long-form WER 0.037, acronyms 0.013, command-mode/mishearing/cleaner all pass; vocab prompt net-positive). Report at `/tmp/ld-campaign/REPORT.md`.
- OPEN: **#3 tail-pad + #4 normalize NOT live-mic-verified** (harness bypasses the recorder) — need one real dictation. **NEW BUG (Ammiel reported 2026-06-21): cold-start LEADING clip** — first words of the FIRST dictation of the day dropped (e.g. "I know every project…" → start mangled); did NOT reproduce on a warm/subsequent dictation. Cause: AVAudioEngine/mic warmup — fresh engine per recording (`AudioFileRecorder.startRecording`), `engine.start()` returns before the tap streams audio. Distinct from #3 (tail). **Next task → v0.3.2.** Deferred decoder tuning #5 beam / #6 thresholds / #7 mc (campaign saw ZERO repetition loops → low urgency). Merged `feat/turbo-accuracy` branch not deleted.

## SHIPPED (2026-06-21) — Context-aware dictation P1–P4 RELEASED as v0.3.0
- PR **#1** (`feat/context-aware-dictation`) squash-merged to **main @ `03b63f3`**; **release v0.3.0** published (Latest) — DMG + zip + signed Sparkle appcast live at `releases/latest/download/appcast.xml`, so installed apps auto-update. v0.3.0 (build 28) installed + running at /Applications. Last pre-feature release was v0.2.3.
- Whole arc (P1 recognition bias, P2 command mode me→main, P3 AX window text, P4 opt-in OCR, + mic-less `LD_PIPELINE_TEST` harness) is in `03b63f3` — see the build/verify detail in the block below. 48 unit tests, build-app green; live-verified (real voice + mic-less WAV harness). Diagnostic env-hooks (`LD_CONTEXT_PROBE`, `LD_PIPELINE_TEST`, `LD_PROBE_OCR*`) ship release-available but inert without the env var (a deliberate, documented test surface — could be #if DEBUG-gated later if undesired).
- OPEN/leftover: worktree `~/work/tools/local-dictation-context` (merged feat branch) can be removed; remote feat branch not deleted. Ammiel granted Screen Recording to the app for OCR testing but `useScreenOCR`=OFF by default (revocable, no effect on AX-first).

## (superseded — now merged/released as v0.3.0) END-OF-SESSION STATE (2026-06-20, later) — Context-aware dictation P1+P2+P3+P4 BUILT
- Branch **`feat/context-aware-dictation`** (worktree `~/work/tools/local-dictation-context`), HEAD `c452215`, **committed locally, NOT pushed/merged**. Off `main` @ `3276304`. Built + signed via `scripts/build-app.sh` and **installed + running at /Applications** (replaced the v0.2.3 install).
- **P3 (AX visible-window text) + P4 (opt-in OCR) ADDED** (commit `c452215`):
  - P3: `AXSupport.swift` (shared AX wrappers + the secure-field guard as ONE chokepoint; CaretContext's isSecure deduped into it). `WindowTextReader.swift` (bounded BFS over the focused window subtree — AXTextArea/AXTextField/AXStaticText values, suffix for big values, maxChars 1600/maxNodes 600 + a 30ms wall-clock budget so it never stalls mic startup; secure skipped). Filled into `DictationContext.visibleText` → `ContextBias.candidates` already turns it into recognition-bias candidates. RUNTIME-VERIFIED (probe, signed bundle): TextEdit doc → 5 candidates (feature/context-aware, UserStore.swift, dataLoader.ts, v0.2.3, p3doc.txt). GOTCHA: **Terminal.app does NOT expose scrollback via AXValue** (only the caret line via range queries, which P1 uses) → P3 gets mostly title chrome there; this is exactly why OCR (P4) exists as the Terminal fallback.
  - P4: `ScreenOCR.swift` (ScreenCaptureKit `SCScreenshotManager` window capture at native Retina scale + Vision `.accurate`, languageCorrection off to keep identifiers; Screen Recording permission gated at one chokepoint, graceful nil without it). `useScreenOCR` setting (**default OFF**, opt-in) + Advanced toggle (requests permission on enable). `AppModel.enrichWithOCR`: merges a fresh cached OCR for the same app + kicks off a THROTTLED ASYNC refresh — **never blocks the mic**; only runs when AX gave <24 chars AND permission held. Named consts (ocrCacheTTL 30s, ocrRefreshThrottle 6s). RUNTIME-VERIFIED: Ammiel granted Screen Recording → `screenRecordingPermission=true`, OCR captured a TextEdit window and Vision read its full text cleanly ("...git push origin main branch feature/context-aware files: UserStore.swift dataLoader.ts release v0.2.3 to Vercel"). 1x+`.fast` gave garbled output → fixed to Retina-scale + `.accurate`.
  - TESTS: `LocalDictationCoreTestRunner` 47→**48** (+visible-text→candidates→prompt). `scripts/build-app.sh` green. Simplify pass (4 agents): applied O(n) BFS head-index, mic-path time budget, OCR permission pre-check, named magic numbers; SKIPPED policy-type extraction + role-list generalization (premature/over-broad). Probe (`LD_CONTEXT_PROBE`, +`LD_PROBE_OCR`/`LD_PROBE_OCR_REQUEST`) extended to report visibleText + OCR + a synthetic-image Vision self-test.
  - NOTE: Screen Recording is now GRANTED to the app (Ammiel approved for the test) but `useScreenOCR`=OFF, so OCR never runs until he enables it. He can revoke the permission with no effect on AX-first behaviour.

### Earlier this session — P1+P2 (committed `7e05e86`, docs `5df0fff`/`17c9d56`/`4f957f6`)
- SHIPPED (this branch): **P1 recognition bias + P2 command mode** per `docs/context-awareness-spec.md` (status block updated at top of that doc).
  - Core (pure): `DictationContext.precedingText`; `ContextBias.swift` (AppClass taxonomy + `classify`, app-class dev/git vocab, identifier-candidate extraction proximity-ordered+deduped, `PromptContext`); `RecognitionContext.prompt` gained `context:` param (folds preceding text → candidates → app vocab, caret-proximity weighted, budget-capped; **no-context path byte-identical** so old tests pass); `CommandModeCorrections.swift` (`me|mane|maine|mein → main`, gated on terminal/editor app + branch-taking git cmd `push|pull|fetch|checkout|switch|merge|rebase|branch|reset|cherry-pick` in the composed `preceding+transcript` line; command formatting = strip trailing `.`, lowercase leading `Git`; **commit/-m excluded**).
  - App: `CaretContext` extended 1 char → preceding LINE (shared `focusedTextElement()` helper, **secure fields never read** — role/subrole `AXSecureTextField`); `AccessibilityContextProvider.swift` (NSWorkspace frontmost + AX role + caret text; AX-first, transient, never logged); `AppModel` gathers context at `beginHold` (mic-first preserved — cheap ms AX read), threads into `contextPrompt(settings:context:)` (final + preview) and `makeWorkflow`'s `preCorrect` (global `MishearingCorrections` THEN gated `CommandModeCorrections`); `useContextAwareness` setting (**default ON**, AX-only no new perm) + Advanced-tab toggle.
- TESTS: `swift run LocalDictationCoreTestRunner` **47 pass** (was 40; +7: app-class classify, candidate extraction, command-pattern detect, context-folded prompt, and the key pair `me→main` fires in command ctx / LEFT ALONE in prose — both pure AND end-to-end through `DictationWorkflow` incl. cleanup-on caps/period interplay). `scripts/build-app.sh` GREEN (compiles LocalDictationApp — the v0.2.3 trap).
- RUNTIME VERIFIED (no mic) via env-gated `LD_CONTEXT_PROBE` (commit `42c0976`, in `runContextProbe`): launch the SIGNED bundle through LaunchServices (`launchctl setenv LD_CONTEXT_PROBE <file>; open <app>`) so AX trust carries — a direct-exec of the inner binary is NOT AX-trusted (that was the gotcha). Probe logs `AXIsProcessTrusted`, then per-tick frontmost app + class + caret text + command-mode result. PROVEN LIVE in installed bundle, real focused fields: `AXIsProcessTrusted=true`; **Terminal + caret after "git push origin " → me→main (FIRES)**; **Terminal + "please remind " → me→me (gated off, no git command — proves the command-PATTERN gate, not just app class)**; Brave Browser → class=browser, caret=nil (graceful AX-first fallback for Chromium/textless apps, no crash). Set up the terminal cases WITHOUT typing into a terminal (tier-blocked) via a `.command` file running `read -p "git push origin " x` so the prompt sits at the caret. The ONLY unverified step is whisper's ASR (spoken "main" → heard "me"), which is irreducibly the user's voice.
- **LIVE VOICE VERIFIED (real audio through whisper).** Played TTS "push to me" through speakers; the app's raw `AVAudioEngine` mic tap has NO echo cancellation so it captures speaker output. Drove dictation via the SIGUSR1 toggle control surface (`kill -USR1 <pid>` start/stop), preview-or-insert, read the corrected transcript from the `dictationHistory` UserDefault. Same audio, three contexts: **Terminal (caret after "git push origin ") → "Push to main."** (me→main FIRES) · **TextEdit (frontmost confirmed) → "Push to me."** (stays me) · **Brave Browser → "Push to me."** (stays me). The literal two-part acceptance test PASSED end-to-end through the real mic + bundled whisper-server + context pipeline. (Restored: volume, pasteOnRelease/cleanUpTranscript/useContextAwareness defaults, cleared test history.) Trick to set up a terminal caret WITHOUT typing into a terminal: `.command` running `read -p "git push origin " x`.
- **NOT YET DONE / OPEN:**
  2. ~~live-confirm the runtime AX path~~ DONE (probe above — AXIsProcessTrusted + reads "git push origin " from a real Terminal, fires me→main).
  3. Not built: **P3** (walk focused window AX tree → `visibleText` candidates; `ContextBias` already consumes `visibleText` if a provider fills it), **P4** (OCR opt-in fallback), **P5** (context-grounded LLM substitution).
  4. Not pushed, not merged to main, **no release cut**. Merge + `scripts/release.sh vX.Y.Z` when live-verified.
- DECISION: `useContextAwareness` default ON (spec open-Q leaned "on" for P1; AX already used for paste insertion, no new permission, secure-field-safe). OCR (P4) would be the strictly-opt-in default-off one.

## NEXT TASK (P1+P2 DONE on branch — see state above) — Context-aware dictation (fixes "push to main" → "push to me")
- Full design: **`docs/context-awareness-spec.md`**. Build it well and **TEST IT THOROUGHLY** (unit + live real-voice — see testing bar below).
- Problem: a homophone where the wrong word ("me") is the common one AND too common to remap globally; the cleanup-only polish won't substitute. Fix = on-device context: frontmost app + caret-preceding text + AX visible-window text (OCR only as opt-in fallback) → (a) Whisper recognition bias, (b) a context-scoped "command mode" correction (e.g. terminal + caret after `git push origin` → map `me`→`main`).
- Seams READY: `DictationContext` fields already defined in `Sources/LocalDictationCore/ContextProvider.swift` (only `EmptyContextProvider` exists → add a real `AccessibilityContextProvider`); extend `CaretContext` (`Sources/LocalDictationApp/CaretContext.swift`, 1 char → preceding line); extend `RecognitionContext.prompt`; add a context-gated command-mode variant alongside `MishearingCorrections`; wire the provider into `AppModel.contextPrompt`/`makeWorkflow`.
- START: P1 = frontmost app + caret text → recognition bias (no substitution, lowest risk), then P2 = command-mode substitution. Privacy: on-device, opt-in, transient (extract + discard, never log), skip secure fields; AX-first (no Screen Recording perm), OCR opt-in fallback.
- TESTING BAR: deterministic unit tests in `LocalDictationCoreTestRunner` (candidate extraction, command-pattern detection, `me→main` fires in command context but is LEFT ALONE in prose); `swift run LocalDictationCoreTestRunner` (currently 40 pass) AND `scripts/build-app.sh` (the test runner does NOT compile LocalDictationApp — AppModel errors slip past tests, bit us on v0.2.3). Live: dictate "push to me" in a terminal (must → "main") vs Slack/Notes (must stay "me").

## END-OF-SESSION STATE (2026-06-20) — v0.2.3 SHIPPED: "clot→Claude" fixed (display + corrector) + cleanup-only polish + hardened installer
- Repo `~/work/tools/local-dictation`, branch **main** @ `3993ce5` (pushed). Release **v0.2.3** published (DMG + zip + Sparkle appcast). Installed + running at /Applications (v0.2.3 build 25). Tests: `swift run LocalDictationCoreTestRunner` (40 pass). NOT yet confirmed on real voice (needs user mic) — pipeline + live-model proven.
- BUG: user "still seeing clot for Claude". **ROOT CAUSE #1 (display, the real one):** `DictationWorkflow.finishRecording` set `lastTranscript`/`.pasting` from the RAW pre-polish transcript, so history/menu-bar/overlay showed "clot" even though the POLISHED "Claude" was what actually got typed (only `inserter.insert(insertText)` saw the fix). FIX: capture `correctedTranscript = insertText` AFTER polish (before postProcess snippet expansion) and surface THAT everywhere.
- **ROOT CAUSE #2 (corrector unreliable):** the 3B Qwen polish swapped already-correct names for unrelated vocab terms ("Claude Code"→"Vibe Coding", "clot"→"VAD") because the prompt licensed substitution against a busy vocab list. FIX: new `Sources/LocalDictationCore/MishearingCorrections.swift` — deterministic whole-word map (cloud code→Claude Code, claud/clawd→Claude via `TextReplacements`; clot→Claude via a hoisted `static let clotRegex` negative-lookbehind `(?<!\bblood )\bclot\b` that SPARES "blood clot"; "clots"/"clotting" safe by word boundary). Runs as **`preCorrect`** (new optional closure on DictationWorkflow, mirrors `postProcess`) BEFORE polish. LLM polish is now **FORMATTING-ONLY**: `TranscriptPolisher.systemPrompt()` dropped the vocab/`context` param + substitution license (also removed `context` from `LlamaPolishEngine`/`ServerBackedPolisher`); new cleanup few-shot. Gated `cleanUpTranscript || polishWithAI` (free, works even with AI off). Whisper recognition bias (vocab) unchanged — it still biases what whisper hears.
- VERIFIED end-to-end against the LIVE Qwen server (:49956): clot→Claude, Claude Code preserved, blood clot spared, cloud cover spared, fillers/stutters cleaned. Decision (user-approved): cleanup-only over keep-aggressive-substitution. "blood clot" guard added after user asked about genuine "clot".
- **INSTALLER HARDENED** (`scripts/dmg.sh` `.command` + `release.sh` notes): quit a running copy, error-handle the cp, `xattr -cr`, and fall back to "System Settings → Privacy & Security → **Open Anyway**". Context: a friend on **macOS 15+ (Sequoia/Tahoe)** stayed blocked after a manual `xattr` — Apple removed Terminal-xattr & right-click-Open for .app bundles; only "Open Anyway" or notarization works. Scripts (.command) are still right-click-Openable, so the installer route holds. Verified the published DMG contains the hardened installer. PERMANENT fix = notarization (OPEN item #2; needs paid Apple Dev acct; scaffold `DEVELOPER_ID_IDENTITY`+`NOTARY_PROFILE` ready in build-app.sh/release.sh).
- GOTCHA: `swift run LocalDictationCoreTestRunner` only builds **LocalDictationCore** — `AppModel` (LocalDictationApp target) is NOT type-checked by it, so a `@Sendable` closure-conversion error passed tests and only `scripts/build-app.sh` caught it. Build the **app target**, not just tests, before shipping. (Fix: `{ @Sendable text in MishearingCorrections.apply(to: text) }`.)
- ⌃Space is NOT interfering on Ammiel's Mac: input-source-switch symbolic hotkeys 60 (⌃Space) / 61 (⌃⌥Space) are `enabled=false` and only one input source (ABC). OPEN item #1 (⌃Space clash) is a portability concern for OTHER users, not active here.
- simplify pass (4 agents): applied regex hoist (clotRegex `static let`); SKIPPED pre-compiling TextReplacements (microsecond gain on an inference-dominated path) and generalizing `TextReplacements.Rule` for negative-context guards (premature for ONE exception — revisit at a 2nd guarded rule).

## END-OF-SESSION STATE (2026-06-19) — GOAL MET: production-ready, all 6 personas GREEN, tested, friend-bundleable
- Repo: `~/work/tools/local-dictation` (git, branch `feat/local-dictation-mvp`, all UNCOMMITTED — no commits yet). Build: `swift build` / `scripts/build-app.sh` (bundles + signs) / `scripts/package.sh` (→ `dist/LocalDictation.zip` 2.9M). Tests: `swift run LocalDictationCoreTestRunner` (18 pass). App installed + running at `/Applications/LocalDictation.app`.
- DONE THIS SESSION: review→full production push. Settings tabbed (General/Models/Audio/Advanced); premium overlay redesign ("Signal Energy": glass+breathing halo+live waveform+typed DictationPhase, OverlayView.swift/OverlayController.swift); audio meter+device picker; model manager w/ sha256 + friendly errors; menu-bar onboarding callout; a11y labels; stable self-signed cert (perms persist); **whisper-cli + ggml plugins + libomp fully bundled** (rpath surgery + baked-path patch → `/tmp/local-dictation-ggml-backends-dir01` symlinked by `WhisperLocator.ensureBackendsLinked` to bundle Frameworks; VERIFIED self-contained Metal transcription, 0 homebrew deps). 6 persona audits + re-review (Workflow) → all GREEN. Ledger: `docs/production-readiness.md`. Design mockups in easel ("Local Dictation overlay — Signal Energy" #3, system-design #4).
- KEY GOTCHA solved: ggml compute backends are dlopen'd PLUGINS from a 40-char baked path in libggml.0.dylib; redirect via EXACT-length binary patch (null-padding breaks it) + app-maintained symlink. CLT-only (no full Xcode) blocks a from-source static whisper build, hence the plugin-bundle approach.
- whisper-server WIRED UP (2026-06-19, after goal met): eliminates per-call model reload. `Core/WhisperServerEngine.swift` (HTTP `POST /inference` multipart → `{"text"}`, pure/testable, 20 tests now); `App/Audio/WhisperServerManager.swift` (@MainActor: owns the whisper-server Process, POSIX free-port, /-poll readiness, restart-on-model-change, stop on willTerminate). `WhisperLocator.resolvedServer()`. AppModel warms up the server at init + before each dictation; `transcriptionEngine(settings:timeout:)` prefers server (baseURL ready) else CLI fallback; BOTH preview + final passes use it. build-app.sh bundles + signs whisper-server (same rpath surgery). VERIFIED: app auto-starts bundled whisper-server on launch (resident model, auto free-port); bundled server self-contained (0 homebrew refs) transcribes over HTTP. dist zip now 3.3M. NOTE: SIGKILL orphans the server (normal Quit cleans up via willTerminate); next launch picks a fresh port so no collision.
- CRASH FIX (2026-06-19): SIGTRAP `_swift_task_checkIsolatedSwift` on opening Settings→Audio tab. ROOT CAUSE: `AudioLevelMeter` is `@MainActor`, so its `installTap` closure INHERITED main-actor isolation; AVFAudio calls it on the realtime audio thread → main-thread assert fails. The REAL fix is marking the tap closure `@Sendable` (forces it non-isolated) — `{ @Sendable buffer, _ in ... }` — PLUS `nonisolated static` on `normalizedLevel` (so the @Sendable closure may call it). Marking only the func nonisolated was INSUFFICIENT (first attempt still crashed). PROVEN via a DEBUG `LD_METER_TEST` harness run on a cert-signed bundle (mic-authorized): tap fired 2.5s, isRunning=true, level computed, exit 0. (AudioFileRecorder was already safe — not @MainActor → its tap closure is non-isolated by default; that's the pattern.) Lesson: in a @MainActor type, any closure handed to a realtime/background callback must be `@Sendable`.
- HEALTH-STRIP PILLS FIX: the Model pill wrapped to 2 lines (long `ggml-base.en.bin`). Fix: ReadinessModel strips `ggml-`/`.bin` → "base.en"; HealthStripView pills now use a custom `FlowLayout` (Layout protocol) + `.lineLimit(1).fixedSize()` so pills reflow as a row instead of breaking text. Screenshot-verified one-line.
- OPEN (acceptable minors, NOT blockers): normalizedLevel unit test (private App func), string-only clipboard restore, workflow-guard TOCTOU (safe @MainActor), default modelPath string. NOT verified live: real hold-⌃Space-and-speak (needs user mic) — pipeline proven via tests + bundled-whisper e2e. NOT committed to git.


- Project created at `/Users/ammielyawson/work/tools/local-dictation` on branch `feat/local-dictation-mvp`.
- SwiftPM app, not Xcode project. Full Xcode is not selected; `xcodebuild` fails with Command Line Tools only.
- KeyboardShortcuts is pinned to exact `1.12.0` because `2.4.0` uses SwiftUI `#Preview` macros that fail without full Xcode preview macro plugins.
- Automated tests use `LocalDictationCoreTestRunner` instead of `Testing`/`XCTest` because this toolchain does not expose either module.
- V1 is strict-local: microphone audio -> local `whisper-cli` -> overlay preview -> paste insertion with clipboard restore.
- Live preview added (2026-06-19): `AudioFileRecorder` rewritten from `AVAudioRecorder` to an `AVAudioEngine` input tap + `AVAudioConverter` → accumulates 16kHz mono Float samples in memory (lock-guarded), so audio can be snapshotted mid-record (`snapshotForPreview`, last 30s). `AppModel.startPreviewLoop` runs a ~0.9s-cadence whisper-cli pass (8s timeout) on the rolling snapshot, updating the overlay detail with partial text; final authoritative full-file pass on release is unchanged. Partials are lower-quality and self-revise (inherent to short-window whisper); each preview spawns a fresh whisper-cli (model reload churn — a persistent whisper-server would be the perf upgrade). Live mic capture NOT verifiable headlessly — needs user to grant mic + speak.
- Shortcut now ⌃Space (keyCode 49 / modifiers 4096). Caveat: macOS may use ⌃Space for input-source switching.
- Settings redesign Steps 1–3 BUILT (2026-06-19), clean build + 10/10 tests, bundle relaunched. (1) Deployment target 13→14 + AppModel/OverlayState migrated ObservableObject→@Observable (StateObject→State, ObservedObject→plain). (2) `Readiness/ReadinessModel.swift` (@Observable) checks whisper-cli/model/mic/accessibility → `HealthStripView.swift` pills, refreshed onAppear + didBecomeActive. (3) `Models/WhisperModel.swift`+`ModelCatalog` (seeded from research: large-v3-turbo RECOMMENDED, +q5_0, medium.en, small.en, base.en, HF resolve URLs) + `Models/ModelStore.swift` (@Observable; scans model dir = parent of configured modelPath; URLSessionDownloadTask + progress KVO, atomic move; select() writes modelPath UserDefaults) + `Models/ModelManagerView.swift`. `SettingsView` rewritten = grouped Form (health strip + shortcut + model manager + output + advanced paths). Shared `Support/PathUtils.swift` (hoisted expandingTildeInPath, dedup). Core: added `WhisperCLIConfiguration.beamSize` (default 1=greedy, emits `-bs/-bo`) per research (~2x faster, fine for clean dictation).
- MODEL RESEARCH verdict (M4 Max/36GB): single `ggml-large-v3-turbo.bin` (1.62GB) for both preview+final (~0.5s/10s clip, halves base.en WER); no two-model split needed unless preview lags → then turbo-q5_0/small.en. flash-attn default-on in 1.9.1; greedy `-bs 1`. Future spike: Parakeet via whisper.cpp 1.9 (~80ms). Steps 4 (audio meter + device picker) NOT built. Live mic still needs a real hold-to-dictate test by the user.
- Settings redesign VISUALLY VERIFIED (2026-06-19) via an in-app screenshot hook: `AppDelegate` checks env `LD_SCREENSHOT=<path>` → renders `NSHostingView(SettingsView())` in a window → in-process `cacheDisplay` → PNG → exit. No screen-recording perm, no automation layer (computer-use rejected the unsigned bundle as not-installed even from /Applications — its app registry is cached at MCP-server start). Confirmed: health strip 4 green pills, ⌃Space recorder, all 5 models with Recommended/Lighter badges + Download/Active states, output toggles. Hook is dev-only + env-guarded, left in. App now installed at /Applications/LocalDictation.app (running from there).
- TWO launch/UX bugs fixed (2026-06-19): (1) "App won't open / damaged" — `build-app.sh` copied the swift-build-signed binary then added icon+Info.plist AFTER, invalidating the signature (`spctl`: "code has no resources but signature indicates they must be present"); macOS refused Finder double-click. FIX: `build-app.sh` now `codesign --force --deep --sign -` the assembled bundle last + verifies. (2) "Settings won't open" — MenuBarView used `NSApp.sendAction(showSettingsWindow:)` which is unreliable for `.accessory` menu-bar apps (no responder / opens behind). FIX: `@Environment(\.openSettings)` action + flip `setActivationPolicy(.regular)` + `NSApp.activate()` on open; AppDelegate observes `NSWindow.willCloseNotification` and reverts to `.accessory` when no titled window >300pt remains (keeps Dock icon from lingering). Reminder: LSUIElement app = NO window/Dock icon on launch, only the menu-bar mic icon.
- STABLE SIGNING set up (2026-06-19) — fixes permissions resetting every rebuild. Root cause: ad-hoc (`-`) signing gives a new cdhash per build → macOS sees a "new app" → Mic/Accessibility grants drop. FIX: `scripts/setup-signing.sh` (run once) creates a trusted self-signed code-signing cert "Local Dictation Dev" in login keychain (GOTCHA: must use `/usr/bin/openssl` LibreSSL + non-empty p12 pass — Homebrew OpenSSL 3 p12 fails macOS `security import` with "MAC verification failed"). `build-app.sh` now signs with that identity (ad-hoc fallback if absent). Designated requirement is now STABLE across rebuilds: `identifier "dev.ammiel.local-dictation" and certificate leaf = H"fb8ba3b886270f122cd22b2e7a7559c5d10cae0e"` → TCC grants persist. User must grant Mic+Accessibility ONCE more (identity changed ad-hoc→cert; TCC reset) then they stick. Only ONE copy at /Applications; never launch the tools-folder build output (caused the duplicate-identity permission mess).
- GOAL (2026-06-19): production-ready, ALL personas green, tested, bundle-able for friends. Tracking ledger: `docs/production-readiness.md` (5 personas: Dana=release/distribution, Quinn=QA/test, Sasha=security/privacy, Tom=code/concurrency, Robin=UX/a11y; each 🔴→🟢). Process: audit → fix on main thread → re-review to green → live test → distributable bundle + friend install instructions. 5 audit agents dispatched in background.
- Step 4 BUILT: audio meter + device picker. `Audio/AudioDevices.swift` (CoreAudio input-device enum + UID→AudioDeviceID), `Audio/AudioLevelMeter.swift` (@Observable, own AVAudioEngine, RMS→dBFS→0..1, fast-attack/slow-release smoothing; GUARDS on mic auth — touching inputNode without permission SIGTRAPs, exit 133), `Audio/AudioInputSection.swift` (Picker + segmented LevelMeterBar, hint when no mic). Device choice (`inputDeviceUID` setting) applied to both meter + `AudioFileRecorder` via `kAudioOutputUnitProperty_CurrentDevice` on input node's audioUnit. Catalog gained `tiny.en` (fastest); base.en relabeled Faster.
- Settings restructured single-scroll → TABBED (user pref, macOS standard): `Settings/{GeneralTab,ModelsTab,AudioTab,AdvancedTab}.swift` + SettingsView=TabView. General=health strip+shortcut+output; Models=model mgr; Audio=device+meter; Advanced=paths. Screenshot hook (bare NSWindow) renders tab CONTENT but not the Settings-scene tab BAR. OVERLAY redesign still pending (Robin auditing it).
- Visible-screen context and Gemma/local LLM cleanup are deferred, with `ContextProvider` reserved as the integration boundary.
- App icon "Signal" (2026-06-19): white waveform on emerald→teal squircle. Drawn programmatically via `scripts/make-icon.swift` (AppKit/CoreGraphics, no external rasterizer) → `.iconset` → `iconutil` → `AppIcon.icns`. `build-app.sh` regenerates it each build, copies to `Contents/Resources/AppIcon.icns`, Info.plist has `CFBundleIconFile`/`CFBundleIconName`=AppIcon. Menu-bar (LSUIElement) app → no Dock icon; shows in Finder/Get Info/Spotlight. Redesign mockups live in easel session "Local Dictation settings redesign" (#1 settings panel, #2 icon directions). Settings-UI redesign (health strip + model manager + audio meter) is mocked, NOT yet built.
- Review pass (2026-06-19): fixed temp `.wav` leak (`DictationWorkflow.finishRecording` now `defer`-deletes the recording) and added a configurable whisper-cli timeout (`WhisperCLIConfiguration.timeoutSeconds`, default 60s; `runWhisper` watchdog terminates a hung process → `TranscriptionError.timedOut`). Pipe reads now drain concurrently via `WhisperRunCollector` (avoids a latent stdout/stderr pipe-buffer deadlock). New `LocalDictationCoreTestRunner` test "times out a hung process" (10 total, all pass).
- Runtime deps installed on this machine: `brew install whisper-cpp` → `/opt/homebrew/bin/whisper-cli` (1.9.1); model at `~/models/ggml-base.en.bin` (148MB). Verified real transcription end-to-end with the app's exact arg set.
- Default shortcut changed ⌘⌥Space → ⌃⌥; (semicolon) (2026-06-19): ⌘⌥Space is macOS "Show Finder search window" so the OS swallowed it; ⌃⌥D (intermediate pick) collided with a window manager. Settled on ⌃⌥; (carbonKeyCode 41, modifiers 6144). NOTE: KeyboardShortcuts persists the default to UserDefaults (`KeyboardShortcuts_holdToDictate`) after first run, so changing the code default alone won't move an existing install — must `defaults delete dev.ammiel.local-dictation KeyboardShortcuts_holdToDictate` (or rebind in Settings). All Space-based combos are reserved by macOS.
- Open review notes (not fixed): mic permission requested twice (beginHold + recorder), fast tap-release race (isRecording set before beginRecording awaits), stringly-typed overlay state, settings paths are unvalidated text fields, no cancel/Escape.

## SESSION END (2026-06-19, late) — output cleaning + preview de-hallucination
- Insertion cleaning: only text containing a letter/number is typed. `WhisperTranscriptParser.strippedForInsertion` strips whisper non-speech annotations ([…], (…), *…*/**…**, ♪…♪) AND returns "" if no alphanumeric (drops bare "."/". ." silence). DictationWorkflow gates insertion on this → non-speech routes to the "No speech detected" path (nothing typed). Overlay still SHOWS raw transcript (user OK with seeing annotations, just not typing them).
- Preview de-hallucination: startPreviewLoop skips iterations when `recorder.currentLevel <= 0.05` (silence) and runs strippedForInsertion on preview text. Reduces phantom words/punctuation flashing during no-input.
- KNOWN: single-word mis-transcription ("whistle"→"whisper") is inherent ASR (close phonemes, no context); turbo is the mitigation, not a code bug.
- OPEN / NEXT: wire up VAD (the real non-speech fix). whisper.cpp 1.9.1 supports `--vad --vad-model <silero ggml>`. Bundle a Silero VAD ggml (~1-2MB) in build-app.sh, pass the flags in WhisperCLICommand.arguments + WhisperServerManager launch args, verify server honors it. Then VAD drops non-speech segments so whistle/hum/silence → nothing even when whisper would hallucinate a word, and kills trailing-silence/um artifacts. Tests still 21 pass. NOTHING committed to git.

## VAD DONE (2026-06-19, end) — non-speech now produces nothing
- Wired up Voice Activity Detection. Bundled Silero VAD ggml (`ggml-silero-v5.1.2.bin`, 885KB) into `Contents/Resources/` (build-app.sh; downloads from HF ggml-org/whisper-vad if absent). `WhisperLocator.resolvedVadModel()` (bundled Resources else ~/models). `WhisperCLIConfiguration.vadModelPath` → `WhisperCLICommand.arguments` appends `--vad -vm <path>`. `WhisperServerManager` launch args append `--vad -vm <path>`. AppModel.makeConfiguration passes it. VERIFIED e2e: running bundled whisper-server has `--vad -vm .../Resources/ggml-silero-v5.1.2.bin`; speech→transcribes, SILENCE→`{"text":""}` (was "you" before VAD). Both server + CLI-fallback paths covered. dist zip now 4.1M. 21 tests pass. The earlier strippedForInsertion cleaning + preview level-gate remain as belt-and-suspenders. NOTE: VAD flags `-vt`(threshold .5), `-vsd`(min silence), etc. available for tuning if it ever clips soft speech. Still NOTHING committed to git.

## END-OF-SESSION STATE (2026-06-20) — in git + public repo, accent-tested
- Repo NOW in git + PUBLIC: github.com/AmmDuncan/local-dictation (gh acct AmmDuncan), branch `main`, HEAD `3886a0b`. Build: `swift build` / `scripts/build-app.sh` / `scripts/package.sh` / `scripts/release.sh <vX.Y.Z>` (signed Sparkle appcast → GitHub release). Tests: `swift run LocalDictationCoreTestRunner` (28 pass). Reinstall gotcha unchanged.
- SHIPPED this session (all committed/pushed):
  1. Auto-update: Sparkle integrated (bundled+signed, "Check for Updates" menu, daily checks), v0.1.0 released w/ signed appcast (feed = .../releases/latest/download/appcast.xml, SUPublicEDKey in build-app.sh, private EdDSA key in keychain). Notarization SKIPPED (no Apple Dev acct — friends use right-click-Open).
  2. Overlay: tail transcript (latest words pinned bottom, older fade/scroll out — `TailingTranscript`); waveform scroll slowed to 20Hz (`WaveBars` @State timer).
  3. Reliability: error-3 cold-start fix (`TranscriptionError: LocalizedError`; `ResolvingTranscriptionEngine` awaits resident server before CLI fallback; CLI fallback on non-empty server error); degenerate-recording guard (`DictationWorkflow.minimumAudioBytes` 1024 → "no speech" not HTTP 400); FRESH `AVAudioEngine` per recording (fixes headphone/route-change dead-mic — USER-CONFIRMED working); level-meter now tab-scoped (`AudioTab.isActive` + resign-active stop) so mic frees on leaving Audio tab (BT A2DP restores).
  4. Polish pipeline: Tier 1 deterministic `TranscriptCleaner` (fillers/caps/spacing, DEFAULT ON, meaning-preserving). Tier 2 LLM polish = Qwen2.5-3B via HOMEBREW llama.cpp (`LlamaServerManager`, `TranscriptPolisher`/`LlamaPolishEngine`, guardrail) — KEPT but DEFAULT OFF (user turned it off: `defaults write dev.ammiel.local-dictation polishWithAI false`). `PolishModelStore` in-app download flow (sha256-pinned).
  5. Context biasing (the real mishearing fix, no LLM): `RecognitionContext` builds whisper `--prompt` from custom vocabulary + persisted rolling history (UserDefaults `dictationHistory`), threaded into CLI (`WhisperCLIConfiguration.prompt`) + server (multipart `prompt`). Settings→Advanced: vocab field + history toggle. Onset fix: `beginHold` starts capture BEFORE server warmup. Cleaner previews: cleanup applied + skip-unchanged (no flicker).
- ACCENT TEST (parallel workflow, bundled whisper-cli + turbo + VAD): 36/36 across US/GB/IE/IN/AU/ZA (max WER 0.18, all from filler/normalization noise); 5/5 edge cases (empty/truncated→400, clipped-onset loses 1st word, silence→empty, normal→200); context-bias PROVEN: "Quorbix Flowsynth" mis-heard as "Corbix Flosynth" WITHOUT --prompt → exact WITH --prompt. This validates the vocab/history lever for "testing not justin".
- OPEN / NEXT:
  1. Group 4 (agreed, NOT built): make LLM polish an OPT-IN "fix mishearings using history" CORRECTOR — looser guardrail (allow word changes), feed `RecognitionContext` to the polish prompt, a mode setting. Risk = the "adds things" behavior; keep it opt-in.
  2. Bundle `llama-server` + its dylibs (+ openssl, ggml is shared w/ whisper) in build-app.sh so polish works for FRIENDS (currently homebrew-only → friends get no polish, graceful). Qwen download flow already exists.
  3. Dedup: `LlamaServerManager`≈`WhisperServerManager` + `PolishModelStore`≈`ModelStore` → shared resident-server + FileDownloader (flagged in code comments).
  4. Live re-test onset fix with headphones (USER to confirm first-word no longer clips on fast press-talk).

## UPDATE (2026-06-20, later) — vocabulary + punctuation resolved
- HEAD still `cb23b7e`, working tree CLEAN. Changes below are runtime UserDefaults + a reverted feature (no new commits).
- Custom vocabulary CONFIRMED on real voice: "vibe coded" (a 2025 coinage whisper renders as "webcoded" — no ASR model knows it; whisper is 2023-era and there's no newer open one) is now caught with vocab set. This is THE on-device, no-LLM mishearing fix. User UserDefault: customVocabulary = "vibe coded, vibe coding, speech to text, dictation, transcription, Whisper, VAD".
- Built then REVERTED a word-replacement-rules feature (`TextReplacements`) — user prefers vocabulary, no manual replace lists. Reverted to cb23b7e (gone).
- PUNCTUATION finding: whisper DOES punctuate natively but from PROSODY (pauses/intonation) → unreliable on run-on/flat real speech. VERIFIED our pipeline does NOT strip it (VAD vs no-VAD output identical + both fully punctuated; TranscriptCleaner only normalizes spacing; prompt doesn't degrade clean audio). Reliable fix = LLM polish (punctuates from MEANING: adds ?, list commas, periods; preserves words via guardrail) — VERIFIED through the app's llama-server.
- LLM polish RE-ENABLED on user machine for punctuation: polishWithAI = true (~2GB resident RAM + ~150ms/dictation; FINAL text only, preview stays whisper-native). Shipped code default is still OFF (opt-in); user has it ON via UserDefault. Toggle: Settings → General.
- Pipeline now: whisper(+VAD + vocab/history --prompt) → strip → TranscriptCleaner (deterministic, on) → LLM polish (Qwen-3B, on, meaning-based punctuation, word-preserving).
- OPEN (priority reordered): (1) bundle llama-server for friends — polish is now relied on, so distribution needs it (build-app.sh; ggml shared w/ whisper, +openssl); (2) opt-in LLM CORRECTOR mode for mishearings (looser guardrail, distinct from current word-preserving formatter); (3) dedup server managers + model stores.

## UPDATE (2026-06-20, even later) — llama-server BUNDLED (open item #1 DONE), commit e5ecfb3
- DONE item #1: `build-app.sh` bundles llama-server self-contained → LLM polish works for FRIENDS with zero Homebrew. New `bundle_llama()` runs right after `bundle_whisper`. Branch `main`, HEAD now `e5ecfb3` (NOT pushed yet — local only, awaiting user OK to push).
- WHAT it does: copies `llama-server`→Helpers/; its 4 OWN dylibs (`libllama.0`, `libllama-common.0`, `libmtmd.0`, `libllama-server-impl`) + openssl (`libssl.3`/`libcrypto.3` — llama links them for HF downloads we don't use, but they must resolve at load)→Frameworks/. rpath surgery: abs ggml/openssl refs → `@rpath`, `-id @rpath/...` on each dylib, `+@executable_path/../Frameworks` rpath on the binary, `+@loader_path` on the dylibs. libssl→libcrypto `-change` uses the versioned **Cellar** path (captured dynamically via otool grep, not the opt symlink).
- ggml is SHARED with whisper (both link the SAME `/opt/homebrew/opt/ggml/lib/libggml.0.dylib`, current 0.15.1) → libggml + libggml-base + the Metal/CPU/BLAS `.so` plugins + libomp are NOT re-copied. llama loads whisper's already-baked-path-patched libggml via the `/tmp/local-dictation-ggml-backends-dir01` symlink (`LlamaServerManager.start` already calls `ensureBackendsLinked`). So NO separate ggml-plugin byte-patch for llama. Existing nested-then-deep `codesign` loop (`Frameworks/*.dylib` + `Helpers/*`) already signs the new files — no signing change.
- CODE was already prepped (so this was a pure build-script change): `WhisperLocator.resolvedLlamaServer()` already checks `Contents/Helpers/llama-server` first then Homebrew; `LlamaServerManager.start()` already calls `ensureBackendsLinked()`.
- VERIFIED (built v0.1.0 build 9, "Signature OK"): (a) `otool -L` on all 7 new Mach-Os = 0 `/opt/homebrew`//`usr/local` refs; (b) launched the BUNDLED llama-server with `DYLD_PRINT_LIBRARIES` + real `~/models/Qwen2.5-3B-Instruct-Q4_K_M.gguf` → every lib (libllama*, libggml* incl the metal/cpu/blas .so plugins, libssl, libcrypto, libmtmd) loaded from INSIDE the .app, 0 from homebrew, Metal backend active; (c) `/health` 200 in ~13s; (d) `/completion` turned "hello how are you doing today" → "Hello, how are you doing today?" (punctuation = the polish use case). Friends still download Qwen on demand (PolishModelStore); polish default stays OFF/opt-in.
- TEST NOTE: to test standalone I manually created the `/tmp/local-dictation-ggml-backends-dir01` symlink → repo bundle's Frameworks (mirrors what the app does at runtime; whisper already proves the symlink mechanism in prod). The app self-heals the symlink to /Applications on next launch.
- OPEN (remaining): (2) opt-in LLM CORRECTOR mode for mishearings (looser guardrail, feed RecognitionContext, mode setting); (3) dedup `LlamaServerManager`≈`WhisperServerManager` + `PolishModelStore`≈`ModelStore` → shared resident-server + FileDownloader. (User-pending: re-test onset fix with headphones.)
- IN FLIGHT (separate ask): a competitor gap-analysis workflow (rival macOS dictation apps vs our feature set) — running, report pending.

## COMPETITOR-GAP FEATURE BUILD (2026-06-20, even later) — big multi-feature drop on branch feat/competitor-gap-features (HEAD 43afc1c, NOT merged, NOT pushed)
- Competitor gap-analysis (22-agent workflow) found we lead on engine+privacy; gaps were at the OUTPUT/WORKFLOW layer. Then /goal "do all recommendations + properly test". All implemented on branch `feat/competitor-gap-features` off main (`ef0b823`). Installed to /Applications (build4). 36/36 unit tests. NOT committed to main, NOT pushed.
- CORE (LocalDictationCore, all pure + unit-tested): `DictationMode.swift` (modes clean/message/email/note/code + **corrector** — looser guardrail, fed vocab/history context); `TranscriptPolisher` mode-aware `chatRequestBody(mode:context:)` + `isFaithfulCorrection` (length-bounded, allows word swaps); `TextReplacements.swift` (post-transcript find-replace/snippets, opt-in); `InsertionFormatter.swift` (mid-sentence lowercase+space given preceding char); `AppProfile.swift`+`AppProfileResolver` (per-app mode by bundle id); `TranscriptHistory.swift` (searchable text-only); `KeystrokeInserter.swift` (CGEvent unicode typing, surrogate-pair safe); `DictationWorkflow` gained `cancelRecording()`+`.cancelled`+`postProcess` hook (replacements run AFTER polish so expansion doesn't trip guardrail).
- APP: Escape-to-cancel (global+local NSEvent monitors, mutually exclusive); **tap-release race fixed** via `isStarting`/`pendingEnd` deferral (begin/end/cancel split into beginHold/endHold/cancelHold/finishCurrent/cancelCurrent); **mic double-prompt removed** (AudioFileRecorder now `PermissionStatus.isMicrophoneAuthorized` check, not re-request); toggle activation mode; `effectiveProfile`/`makeInserter` wire mode+cleanup+polish+insertion; corrector fed `contextPrompt`; SIGUSR1/2 control (`DictationControl`); searchable History window + `TranscriptHistoryStore`; `FirstRunSetup` (fresh installs default to turbo via empty persistentDomain check); reliable `SettingsLauncher`/`HistoryWindowLauncher` (captures SwiftUI openSettings/openWindow in MenuBarLabel → fixes the dead overlay "Open Settings" button).
- OVERLAY (user-reported): shadow was clipped by panel bounds → added `OverlayController.shadowMargin=40` transparent margin synced with panel size (panelWidth 464, cardHeight+80), shadow softened to radius 24 y12; `TailingTranscript` 2→3 lines + ghost-gradient top fade + eased growth; `.cancelled` phase + showCancelled.
- SETTINGS: General (Mode picker when polish on + Activation hold/toggle + Save history), Advanced (Insertion paste/keystroke + Smart spacing + Text replacements), new **Apps tab** (per-app profiles editor, NSOpenPanel app picker). 8 new AppSettings keys + ActivationMode/InsertionMethod enums.
- KEY FINDING — **corrector solves the user's "Claude→cloud" problem**. Whisper mishears at the ASR step ("cloud" is far more common than the name); old polish was word-PRESERVING so couldn't fix it; Gemma wouldn't help (Qwen already knows "Claude" — not a recency gap). First corrector prompt was too conservative (left "cloud"). Strengthened prompt: "speaker's known terms — REPLACE similar-sounding common words with them". VERIFIED live via bundled llama-server+Qwen: "ask cloud to refactor"→"ask Claude to refactor", "cloud and quinn"→"Claude and Qwen", BUT "the cloud cover is heavy today"→stays cloud (no over-correction). To use: Settings→General→Polish on→Mode=Corrector + Claude/Qwen in vocab. Code mode also verified: "camel case get current user...snake case retry count"→"getCurrentUser...retry_count".
- BUILD: notarization SCAFFOLD only (build-app.sh DEVELOPER_ID_IDENTITY+hardened-runtime path; release.sh NOTARY_PROFILE notarize+staple) — opt-in, default self-signed path unchanged. **BLOCKED on a paid Apple Developer account** (untestable without it). bash-3.2 empty-array guard `${arr[@]+"${arr[@]}"}` required (default /usr/bin/env bash is 3.2.57).
- Opus taste review (clean state machine, no stuck states) found + I fixed: P1 CaretContext `as!`→CFGetTypeID-guarded (was a hard crash on the opt-in smart-spacing AX path); KeystrokeInserter surrogate-pair split; local escape monitor now retained. P2: AppsTab dead defaultMode binding wired + double-save removed (onChange owns persistence); HistoryView refresh on didBecomeActive; WaveBars count/array desync tied to `waveBarCount`.
- SHORTCUT collision (user asked): ⌃Space IS macOS "select previous input source" — only bites with >1 input source (why it's mostly worked). README line 47 accurate; added in-app help on the Recorder. Default unchanged.
- NOT VERIFIED LIVE (headless limits): overlay shadow/text-flow visual (LD_OVERLAY_SHOT hook needs the .app bundle, DEBUG-only — user will see live); real Escape/toggle/per-app/keystroke/smart-spacing on real voice (needs user). Everything else proven via unit tests + bundled-server functional tests.
- OPEN: (1) merge branch→main + push (awaiting user); (2) notarization needs Apple acct; (3) the original 3 open items from earlier (corrector=DONE now; bundle llama=DONE; dedup managers still open); (4) optional: change default shortcut for friends (collision-free).

## SHIPPED TO MAIN + PUSHED (2026-06-20, latest) — feature branch merged, then 2 follow-ups. main HEAD 07a5eeb, all pushed to origin.
- MERGED feat/competitor-gap-features → main (ff) + pushed. Then 2 more merged+pushed:
- **fix: per-app modes toggle was self-disabled** (`b86c617`). AppsTab had `.disabled(!useAppProfiles)` on the WHOLE form incl. the enable toggle → couldn't turn it on. Scoped the disable to just the per-app list Section. (User-reported with screenshot.)
- **feat: polish-model selector** (`07a5eeb`) — user wanted model selection "like whisper". Refactored `PolishModelStore` from single-Qwen → catalog-based (mirrors `ModelStore`): `struct PolishModel` + `PolishModelCatalog` (Qwen 2.5-3B "Recommended" + Gemma 3 4B-it) + per-model download/verify/select; new `PolishModelManagerView` (mirrors `ModelManagerView`); added "Polish (AI cleanup)" section to Models tab. GeneralTab polish row simplified to active-model status (download/select moved to Models tab). Gemma 3 4B sha256 `882e8d2db44dc554fb0ea5077cb7e4bc49e7342a1f0da57901c0802ea21a0863`, from ggml-org, downloaded to ~/models/gemma-3-4b-it-Q4_K_M.gguf.
- **GEMMA vs QWEN — definitive A/B (fair, like-for-like Q4, both via bundled llama-server):**
  - WITH vocab (corrector): **Qwen 6/6 @ 0.18s avg; Gemma 4/6 @ 0.25s**. Gemma's 2 fails = OVER-correction ("the cloud cover"→"the Claude cover", "deployed to the cloud"→"to Claude"); Qwen correctly kept genuine cloud. Both sub-second.
  - WITHOUT vocab: NEITHER fixes Claude/Qwen (both leave "Cloud"/"Quinn", just capitalized) → **vocab is the lever, not model recency**. Gemma's only recency win: "webcoded"→"vibe coded" on its own (Qwen→"web coding"); but you'd vocab that anyway.
  - The earlier "Gemma runs away / 30s" was the **Cotypist `gemma-4-E4B-UD-Q5_K_XL.gguf` file's nonstandard packaging** (ChatML-ish embedded template), NOT Gemma — the official ggml-org Gemma 3 4B stops cleanly. The 30s was the runaway hitting max tokens, not real latency.
  - VERDICT: **Qwen 2.5-3B stays the recommended default** (better accuracy, smaller 1.9GB, no over-correction). Gemma offered as a switchable option (fine for clean/email/code modes).
- Polish-model switch is runtime: selecting writes `polishModelPath`; `warmUpPolishServer` restarts llama-server with the new model on next dictation.
- STILL OPEN: notarization (Apple acct); dedup LlamaServerManager≈WhisperServerManager + (now) the polish/whisper stores share a lot — a generic FileDownloader/ResidentServer is the natural dedup; optional collision-free default shortcut.

## BUILT-IN DEFAULT VOCABULARY (2026-06-20, latest) — main HEAD 3b30b6e, pushed
- User asked how Superwhisper "knows Claude" without added vocab → answer: it ships a built-in dictionary. Shipped the same: `DefaultVocabulary.terms` (curated ~29: Claude/Anthropic/ChatGPT/OpenAI/Qwen/Gemini + dev tooling + products + "vibe coding"). Merged into `RecognitionContext.prompt(vocabulary:defaults:history:)` budget-aware (user vocab FIRST → defaults deduped-vs-vocab → history). Feeds BOTH whisper --prompt (recognition) AND corrector context (via AppModel.contextPrompt). Setting `useDefaultVocabulary` default ON, toggle in Advanced.
- VERIFIED: with NO user vocab, corrector now fixes "ask cloud"→"ask Claude" + keeps genuine "cloud cover" (broad default list slightly less punchy per-term than a focused user list — got Claude, missed quinn→Qwen that the 3-term focused list caught). 37 unit tests (1 new). Recognition-level whisper-prompt benefit not headless-testable (needs audio).
- KEY INSIGHT (from no-vocab A/B): model recency does NOT fix name mishearings — vocab is the lever. Built-in vocab = "knows common terms out of the box"; corrector = catches stubborn ones; both now seeded by defaults with zero config.

## v0.2.0 RELEASED + menu/error/dmg polish (2026-06-20, latest) — main HEAD 99ce097, release v0.2.0 published
- **RELEASED v0.2.0** (`scripts/release.sh v0.2.0`) — first release since v0.1.0, carries ALL the session's work. GitHub release assets: `LocalDictation-0.2.0.dmg` (14M, drag-to-install for friends), `LocalDictation.zip` (13M, Sparkle auto-update), signed `appcast.xml`. v0.1.0 users auto-update. https://github.com/AmmDuncan/local-dictation/releases/tag/v0.2.0
- **Menu Mode picker**: MenuBarView shows a Mode Picker (clean/message/email/note/code/corrector) when polishWithAI is on — quick switch without opening Settings. Per-app profiles still override.
- **Friendly errors**: `TranscriptionError.userMessage` (jargon-free; no "whisper-cli", paths, or status codes — those stay in `description` for logs). AppModel routes overlay/menu errors via `userFacingMessage(for:)` (TranscriptionError→userMessage, AudioRecordingError→description, else generic). `emptyTranscript` now = quiet "no speech" (no red error overlay). Regression test asserts userMessage has no jargon. (User-reported: overlay showed "whisper-cli completed but returned no transcript".)
- **.dmg packaging**: `scripts/dmg.sh` (hdiutil, drag-to-install: app + /Applications symlink, dependency-free); `release.sh` now attaches the .dmg alongside the .zip.
- **DISTRIBUTION POLICY (user, 2026-06-20)**: notarization NOT needed — friends know the user personally + accept the Gatekeeper warning. Self-signed is fine. So distribution = cut a release; friends download .dmg → drag → right-click Open (or Settings→Open Anyway). Notarization scaffold stays for if an Apple Dev acct ever appears. On macOS Sequoia self-signed may need Settings→Open Anyway or `xattr -dr com.apple.quarantine` (INSTALL.md covers it).
- 38 unit tests. STILL OPEN: dedup whisper/polish stores + server managers (now even more similar); optional collision-free default shortcut.

## DEDUP + INSTALLER + MODE COLLAPSE → v0.2.2 (2026-06-20, latest) — main HEAD 8982285, release v0.2.2 published
- **Dedup (no behavior change, verified)**: `ResidentServerManager` (one @Observable class + per-server `Config` {args, healthPath, requireHTTPOK}) replaces WhisperServerManager + LlamaServerManager. `CatalogModelStore` base (over `DownloadableModel: Sendable` protocol) holds shared download/verify/select; `ModelStore`/`PolishModelStore` are thin subclasses (catalog + active-path key). Verified: both servers spawn on launch; 37 tests; @Observable inheritance works.
- **Friend install ("damaged" block)**: root cause = self-signed cert (untrusted on friend machines) + quarantine → "damaged ... move to Bin" with NO Open Anyway (worse than "unidentified developer"). Fix WITHOUT $99 Apple acct: strip quarantine. `dmg.sh` now bundles a double-click **"Install Local Dictation.command"** (copies to /Applications + `xattr -dr com.apple.quarantine` + opens) — friend right-click→Opens it once (scripts get Open Anyway, unlike app bundles). INSTALL.md rewritten (accurate: installer OR `xattr` by hand; the old "Open Anyway" step was WRONG for self-signed). release.sh ships dmg + INSTALL.md + step-by-step notes (notes via `--notes-file` — heredoc backticks broke `gh release create` once). User DECISION: doesn't care about the warning (friends know him) → notarization NOT pursued; scaffold stays.
- **MODE COLLAPSE (user-driven)**: removed the whole multi-mode system. User insight: only Corrector ever changed words, so Clean/Email/Code silently failed to fix names ("clot"→"Claude"), and the picker had no descriptions = confusing. A "mode" was just a different system-prompt to Qwen (+ whether vocab was injected — the bug: only Corrector got vocab). NOW: **one polish behavior = always correct mishearings (vocab+defaults context) + cleanup**, guarded by `isFaithfulCorrection`. DELETED: DictationMode, AppProfile/Resolver, AppsTab/ProfilesModel, Mode pickers (menu+Settings), mode/profile settings keys. TranscriptPolisher holds the single corrector prompt (hardened: "keep every other word, only substitute"); LlamaPolishEngine/ServerBackedPolisher always pass vocab context. Corrector prompt VERIFIED via bundled Qwen: "clot"/"cloud"→"Claude" fixed, "cloud cover"/"blood clot" kept.
- **KEY LEARNINGS**: (1) whisper --prompt biasing is PARTIAL for stubborn homophones (Claude→cloud/clot) — the LLM correction is what reliably fixes them, and it must (a) be allowed to change words + (b) be fed the vocab. (2) vocab is the lever, not model recency (no-vocab: neither Qwen nor Gemma fixes names). (3) Gemma 3 4B A/B: Qwen 6/6 vs Gemma 4/6 (Gemma over-corrects genuine cloud); both ~0.2s. Qwen stays default. Polish-model selector (Models tab, Qwen+Gemma) shipped earlier in v0.2.x.
- **RELEASES**: v0.2.0 (all competitor-gap features), v0.2.1 (dedup + installer dmg + correct INSTALL), v0.2.2 (mode collapse). Each: dmg(+installer) + zip(Sparkle) + INSTALL.md + signed appcast. Latest = v0.2.2. Download: https://github.com/AmmDuncan/local-dictation/releases/latest
- **OPEN**: (1) optional collision-free default shortcut (⌃Space = macOS input-source switch); (2) notarization if an Apple Dev acct appears (scaffold ready: DEVELOPER_ID_IDENTITY + NOTARY_PROFILE); (3) optional: re-add a single "Code mode" toggle if the user finds they dictate code often (offered, declined for now); (4) live user verification of clot→Claude on real voice with v0.2.2 installed.
